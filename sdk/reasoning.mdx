---
title: "Reasoning"
sidebarTitle: "Reasoning"
description: "Train reasoning models and read their explanations and counterfactuals."
---

{/* AUTO-GENERATED by scripts/gen_sdk_mdx.py — do not edit. */}

## `op.reasoning.fit()`

```python
fit(df: DataFrame, *, task: Task | None = None, metrics: list[str] | None = None, model_types: list[str] | None = None, hpo_space: HPOSpace | None = None, optimizer: Optimizer | None = None, random_state: int = 42) -> ReasoningFitJob
```

Fit a [`ReasoningModel`](/sdk/model#reasoningmodel) via the OuterProduct API.

Supervised training on `task`'s `label_column`: the resulting
reasoning model produces both predictions and feature-level explanations.

The trainer-style `hpo_space` is mirrored here so a reasoning fit is a
single self-contained call — no need to construct a separate
[`Trainer`](/sdk/trainer#trainer).

### Parameters

<ParamField body="df" type="DataFrame" required>
  Training data.
</ParamField>

<ParamField body="task" type="Task | None">
  The learning task and its target columns (e.g.
`op.Regression(label_column=...)`). Required for supervised fits.
</ParamField>

<ParamField body="metrics" type="list[str] | None">
  HPO objective metric names (e.g. `["neg_rmse"]`, `["auc"]`).
The order is the objective order; passing more than one runs a
multi-objective (Pareto) search. Defaults to the task metric
server-side (neg-RMSE for regression, neg-class-error otherwise).
</ParamField>

<ParamField body="model_types" type="list[str] | None">
  Surrogate model families to fit. A list is accepted for
forward compatibility, but only `"tabm"` and `"tabicl"` are allowed
today — any other value is rejected at request validation. Defaults
to `["tabm"]`.
</ParamField>

<ParamField body="hpo_space" type="HPOSpace | None">
  The surrogate search space (a single model family). Must be
a built [`HPOSpace`](/sdk/hpo#hpospace) — construct
it with [`HPOSpace.from_dict`](/sdk/hpo#hpospace-from_dict)
or `HPOSpace().add(...)` (a raw dict is rejected). Its family must
be `"tabm"` or `"tabicl"`. Defaults to a TabM surrogate.
</ParamField>

<ParamField body="optimizer" type="Optimizer | None">
  How to search the surrogate space — the sampler and a
round-structured trial budget (`n_trials_per_step` × `n_steps`). If
omitted, the server uses a default optimizer.
</ParamField>

<ParamField body="random_state" type="int">
  Random seed for reproducibility.
</ParamField>

### Returns

<ResponseField name="returns" type="ReasoningFitJob">
  Handle to the running fit job. Call [`ReasoningFitJob.wait`](/sdk/jobs#reasoningfitjob-wait) to block for the trained [`ReasoningModel`](/sdk/model#reasoningmodel), or use [`Job.status`](/sdk/jobs#job-status) / [`Job.results`](/sdk/jobs#job-results) to poll. The reasoning model supports [`ReasoningModel.explain`](/sdk/model#reasoningmodel-explain), [`ReasoningModel.get_global_drivers`](/sdk/model#reasoningmodel-get_global_drivers), and [`ReasoningModel.scenario`](/sdk/model#reasoningmodel-scenario).
</ResponseField>

## `Reasoning`

A single explanation: feature drivers plus optional rules.

Returned by [`ReasoningModel.explain`](/sdk/model#reasoningmodel-explain)
and
[`ReasoningModel.get_global_drivers`](/sdk/model#reasoningmodel-get_global_drivers).

### Fields

<ParamField body="feature_names" type="list[str]">
  Names of the raw input features. These align with the
last axis of `drivers` only when the model has no categorical
features. When it does, the model attributes in an encoded space
(categoricals are one-hot expanded), so `drivers` is wider
than `feature_names`; use `encoded_feature_names` to label the
driver columns in that case.
</ParamField>

<ParamField body="drivers" type="np.ndarray">
  Per-feature contribution values, in the encoded feature
space. Shape `(n_samples, n_encoded_features)` for local
explanations, or `(n_encoded_features,)` for global / model-wide
views. The last axis aligns with `encoded_feature_names` when that
is present, else with `feature_names`.
</ParamField>

<ParamField body="encoded_feature_names" type="list[str] | None">
  Encoded column labels aligned to the last axis
of `drivers` (categorical features are one-hot expanded, e.g.
`"State__KS"`). Present only when the model has categorical
features — otherwise `None` and `feature_names` already aligns.
</ParamField>

<ParamField body="rules" type="list[str] | None">
  Human-readable decision rules associated with the prediction
(for example, `"age > 50 AND tenure < 2"`).
</ParamField>

<ParamField body="metadata" type="dict[str, Any] | None">
  Free-form provenance fields — engine name, version, timing,
etc. Not part of the API contract.
</ParamField>

## `ScenarioResult`

Result of [`ReasoningModel.scenario`](/sdk/model#reasoningmodel-scenario).

One [`QueryResult`](/sdk/reasoning#queryresult) per input row, plus
the target class the search aimed for.

### Fields

<ParamField body="desired_class" type="int">
  The class the search tried to flip predictions toward.
</ParamField>

<ParamField body="queries" type="list[QueryResult]">
  Per-input-row results, in the order the rows were supplied.
</ParamField>

## `QueryResult`

Scenario search results for one input row.

### Fields

<ParamField body="query_index" type="int">
  Position of this query in the input dataset.
</ParamField>

<ParamField body="query" type="dict[str, Any]">
  The original input row, keyed by feature name.
</ParamField>

<ParamField body="baseline_prediction" type="float">
  The model's prediction on `query` before any edits.
</ParamField>

<ParamField body="already_at_desired_class" type="bool">
  `True` if the model already predicted the
desired class for this row, so no search was needed; `scenarios`
will be empty.
</ParamField>

<ParamField body="scenarios" type="list[Scenario]">
  Candidate edits found within the search budget. May be empty.
</ParamField>

## `Change`

Single-feature edit between a query and one scenario row.

### Fields

<ParamField body="before" type="Any">
  The feature's value in the original query.
</ParamField>

<ParamField body="after" type="Any">
  The feature's value in the scenario row.
</ParamField>

<ParamField body="delta" type="float | None">
  `after - before` for numeric features; `None` for non-numeric.
</ParamField>
